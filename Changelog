0.06:
	added config vars:
	 - docroot: apid does a chdir() to this directory
	 - pidfile: specify where to store the pid

0.05:
	changed namespace, made module of it,
	apid daemon only uses the module

0.01:
	initial version
